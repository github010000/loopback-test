spring:
  application:
    name: xpg-nxpg-svc

---
spring:
  profiles: local, dev, stg
  cloud:
    config:
      enabled: false
---
spring:
  profiles: jenkins, prd
  cloud:
    config:
      uri: https://common-config-server-dev.skb-suy-dev01.mybluemix.net/